spring.application.name=usuarios

# Puerto en el que la aplicaci�n de Spring Boot escuchar� las solicitudes HTTP
server.port=4000

# Configuramos la URL de conexi�n para la base de datos MySQL
# Desactiva el uso de SSL para la conexi�n
spring.datasource.url=jdbc:mysql://localhost:3306/api?useSSL=false

# Nombre de usuario y password para conectarse a la base de datos
spring.datasource.username=root
spring.datasource.password=

# Hibernate no realizar ninguna acci�n sobre el esquema de la base de datos,
# no crear� ni modificar� tablas
spring.jpa.hibernate.ddl-auto=none

# Configuramos el dialecto de la base de datos que Hibernate utilizar�
# para interactuar con MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Definimos la clase del controlador JDBC que Spring Boot utilizar� para conectarse
# a la base de datos MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
